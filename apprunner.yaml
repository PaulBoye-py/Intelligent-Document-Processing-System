version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip install --no-cache-dir -r requirements.txt
run:
  runtime-version: 3.11.11  # Use supported version
  command: gunicorn --bind 0.0.0.0:8000 --workers 2 modern_app:app
  network:
    port: 8000
  env:
    - name: AWS_DEFAULT_REGION
      value: us-east-1
    - name: DEPLOYMENT
      value: production
    - name: SECRET_KEY
      value: FpEjMYYC5ilS/ApNTrqOCvdX4e8s4NvECFiJ6QQm